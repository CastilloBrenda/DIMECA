{% extends 'base.html' %}

{% block content %}

{% for oferta in ofertas %}
<div>
  <p>publicado: {{ ofertas.fecha_publicacion }}</p>
  <h1><a href="">{{ ofertas.tipo_de_trabajo }}</a></h1>
  <p>{{ ofertas.publicacion|linebreaksbr }}</p>
</div>
{% endfor %}

{% endblock %}
